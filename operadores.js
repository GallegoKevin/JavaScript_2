console.log('// OPERADORES:');

const age1 = 30;
const age2 = "25";
const age3 = 35;

// Operador mayor que (>)
console.log( age1 > age3);

// Operador menor que (<)
console.log(age1 < age3);

// Comparación de igualdad estricta (===)
console.log(age1 === age2); 

// Intento de división por una cadena no numérica
console.log(age1 / "not a number");

const name1 = 'Maria';
const name2 = 'John';

// Operador de desigualdad estricta (!==)
console.log(name1 !== name2); 
